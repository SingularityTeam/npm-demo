/**
 * 模拟一个首页轮播图列表组件，并且位置层级较深
 */

// 引入公共模块，问题在于过多的'../'，不利于代码维护
// import { baseUrl, logger } from '../../../../../common-package-test';

// logger('较多层级的模块引入：' + baseUrl);

import { baseUrl, logger } from 'common-package-test';

logger('npm本地包优雅解决：' + baseUrl);